package com.mitocode.config;

// MapperConfig is no longer needed — MapStruct generates all mapper beans
// via @Mapper(componentModel = "spring") at compile time.
// ModelMapper has been removed from the project.
